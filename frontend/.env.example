# Frontend environment variables for Vite
# If frontend and backend are hosted separately, set this to your backend URL.
# If the frontend is served from the same host as the backend, you can leave this unset.
VITE_API_URL=https://your-backend-host.onrender.com
